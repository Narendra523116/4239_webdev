
async function DebounceUpdate(req, res, delay = 1000){
    let timeout;
    return (...args)=>{
        clearTimeout(timeout);
        timer = setTimeout(()=>{
            time
        }, delay)
    }
}